Building the food ordering system


Components:

Header
    -Logo
    -Nav-items

Body
    -Search
    -RestaurantContainer
        -RestaurantCards

Footer
    -Copyright


Props :
    props are values passed to the Components to make the Component more dynamic
    It is like passing an argument to the normal JS function, but with react component
    The props passed will be get as an object by the component


